import { NombreCompletoAlumnoPipe } from './nombre-completo-alumno.pipe';

describe('NombreCompletoAlumnoPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreCompletoAlumnoPipe();
    expect(pipe).toBeTruthy();
  });
});
