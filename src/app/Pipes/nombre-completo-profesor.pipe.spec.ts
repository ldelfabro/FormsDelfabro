import { NombreCompletoProfesorPipe } from './nombre-completo-profesor.pipe';

describe('NombreCompletoProfesorPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreCompletoProfesorPipe();
    expect(pipe).toBeTruthy();
  });
});
